FROM ubuntu:devel
COPY ./REQUIREMENTS.txt /REQUIREMENTS.txt
RUN apt-get update -y && apt-get install -y locales python3.11-full python3-pip apt-utils openssh-client gnupg2 dirmngr iproute2 procps lsof htop net-tools psmisc curl tree wget rsync ca-certificates unzip bzip2 zip nano vim-tiny less jq lsb-release apt-transport-https dialog libc6 libgcc1 libkrb5-3 libgssapi-krb5-2 libicu[0-9][0-9] liblttng-ust[0-9] libstdc++6 zlib1g locales sudo ncdu man-db strace manpages manpages-dev init-system-helpers software-properties-common build-essential lldb llvm gdb tar zip pkg-config bash-completion && sudo apt-get full-upgrade -y && python3 -m pip install -r REQUIREMENTS.txt --break-system-packages --no-cache-dir && rm -rf /var/lib/apt/lists/* \ && localedef -i en_US -c -f UTF-8 -A /usr/share/locale/locale.alias en_US.UTF-8 && apt-get full-upgrade -y && update-alternatives --install /usr/bin/python python /usr/bin/python3.11 1
ENV LANG en_US.utf8